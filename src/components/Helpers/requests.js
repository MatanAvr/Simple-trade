const BASE_URL = "https://simple-trade-israel-dev.herokuapp.com";

const getPrice = async function (symbol) {
  const url = "";
  try {
    const response = await fetch();
  } catch (err) {}
  return getPrice;
};

module.exports.getPrice = getPrice;
